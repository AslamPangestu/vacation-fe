import Ratings from "components/Ratings";

const Testimonial = () => {
  return (
    <section className="container">
      <Ratings value={2.5} width={46} height={46} />
    </section>
  );
};

export default Testimonial;
